const CarouselTitle = ({ title }) => (
  <div className="flex flex-col gap-4 mb-4">
    <span className="carousel-title text-24-bold text-[#181818]">{title}</span>
  </div>
);

export default CarouselTitle;
